# Upstream changes

`bootable/recovery` (TWRP-Test/android_bootable_recovery, `twrp-16.0`):

- `minuitwrp/libminuitwrp_defaults.go` (commit `83136d6f`) — added the
  `-DUSE_QTI_AIDL_HAPTICS` cflag so the AIDL vibrator path compiles.

Nothing else in `bootable/recovery` is modified.
